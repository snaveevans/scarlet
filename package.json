{
  "name": "scarlet",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.mjs",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "start": "node src/index.mjs",
    "test": "node --test 'tests/**/*.test.mjs'",
    "test:unit": "node --test 'tests/unit/*.test.mjs'",
    "test:integration": "node --test 'tests/integration/*.test.mjs'",
    "test:legacy": "node --test 'tests/*.test.mjs'",
    "prd:new": "node scripts/render-prd.mjs",
    "validate:jsonl": "node scripts/validate-jsonl.mjs",
    "docker:build": "docker build -f docker/Dockerfile -t scarlet .",
    "docker:test": "docker compose -f docker/docker-compose.yml up --build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/snaveevans/scarlet"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
